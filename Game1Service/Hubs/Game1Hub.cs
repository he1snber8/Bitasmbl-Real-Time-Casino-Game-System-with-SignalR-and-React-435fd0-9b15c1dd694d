public class Game1Hub : Hub {\n  // TODO: store table states, players, delayTime\n  public Task JoinGame(JoinGameDto dto){/* add player; if 2 players -> start timer */return Task.CompletedTask;}\n  private async Task StartGameAsync(TableState t){\n    await Task.Delay(TimeSpan.FromSeconds(t.DelayTime));\n    var winner = PickRandom(t.Players);\n    // TODO: notify Lobby (SignalR client or HTTP)\n  }\n}