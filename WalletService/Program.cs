var builder = WebApplication.CreateBuilder(args);\nvar balances = new Dictionary<string, decimal>();\nvar app = builder.Build();\n// TODO: secure so only Lobby can call (e.g. shared secret)\napp.MapPost("/wallet/debit", (Txn t) => { /* update balances */ return Results.Ok(); });\napp.MapPost("/wallet/credit", (Txn t) => Results.Ok());\napp.MapPost("/wallet/rollback", (Txn t) => Results.Ok());\napp.Run();